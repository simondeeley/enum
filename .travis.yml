language: php
php:
  - '7.1'
  - nightly

cache:
  - vendor

before_script:
  - phpenv config-rm xdebug.ini
  - composer install

script:
  - phpdbg -qrr $PATH/phpunit --bootstrap vendor/autoload.php tests
